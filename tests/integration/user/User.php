<?php

// Note that it's vital that the param to User::newFromId and the local variable inside Foo::execute
// have the same name.

class User {
	public static function newFromId( $sameName ) {
		// placeholder
	}
}
